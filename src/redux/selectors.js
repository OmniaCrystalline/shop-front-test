export const isLoading = (state) => state.data.isLoading;
export const basket = (state) => state.data.basket;
export const user = (state) => state.data.user;
export const products = (state) => state.data.products;


